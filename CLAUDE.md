# プロジェクト概要

音楽発見体験を記録・検索するための個人用ツール。
詳細は `docs/` 配下のドキュメントを参照。

- ユーザーニーズ: `docs/user-needs.md`
- ユーザーストーリー: `docs/user-stories.md`
- 設計文書: `docs/design.md`
- UI 設計原則: `docs/ui-principles.md`（作成予定）
- 開発プロセス: `docs/process.md`
- 意思決定ログ: `docs/decisions.md`

## Claude との対話ルール

- **選択肢を提示するときは、各案の利点・欠点を必ず併記する。** ユーザーが判断するための材料を省略しない
- **やり取りの内容をまとめるときは、ユーザーの発言を直接参照して照合する。** 一般的なパターンに当てはめて解釈せず、ユーザーが実際に言った操作・意図を忠実に記述する。特に「○○は不要」「○○しない」という否定の発言を見落とさない

## 現在のステータス

- 企画・設計フェーズ: 設計（Step 4）の第2段階に着手中
- MVP は S1（記録）、S3（編集・削除）、S4（一覧）、S8（リンクから再生）の4ストーリー
- S1 は URL のみで保存可能（即時キャプチャ）。曲名・アーティスト名は任意で後から補完
- S2（URL 自動取得）は次期の最優先。未補完レコードの一括取得モードを含む
- 設計の進め方: 2段階に分けて進行中
  - [x] 第1段階: データモデル + デプロイ形態 — 完了
  - [ ] 第2段階: 画面構成・操作フロー → 技術スタック詳細 — 着手中
- 第1段階で決まったこと:
  - デプロイ形態: ローカル実行（軽量サーバー + SQLite）。バックグラウンド常駐は不採用（使うときに起動、終わったら終了）
  - データモデル: 単一 records テーブル + tags テーブル（中間テーブルなし）
  - URL: 一意制約なし（再遭遇記録のため）、可変（リンク切れ対応）
  - タグ: 保存時に trim → NFKC → lowercase で正規化（日本語の全角/半角統一を含む）。候補表示は次期
  - 検索: 完全一致 + FTS5（trigram トークナイザ。日本語の分かち書き不要）。ベクトル検索は不採用
  - テーブル分割（Song + Encounter）は不採用（同定困難・CRUD複雑化）
  - artist は単一テキストフィールド（LIKE/FTS5 で部分一致検索可能）
  - 日本語テキスト対応: ユーザーは日本人、音楽も日本のものが多い。trigram・NFKC はこの制約への対応
  - 日本語対応には既知の限界あり（3文字未満検索、カナ不一致、エイリアス問題等）。再検討トリガーとして `design.md` に記録済み
- 再検討トリガーの運用を `process.md` に追加済み（摩擦→確認→ストーリー/Issue→設計見直し→記録の流れ）
- 第2段階の進捗:
  - [x] 画面構成: 2画面構成（ホーム画面 + 詳細画面）。ホーム = URL 入力欄 + 一覧、詳細 = 全フィールド表示・編集
  - [x] S1 操作フロー: C 案（URL 保存→詳細画面遷移）。「戻る」操作が保存トリガーを兼ねる（初期設計、変更可能）
  - [ ] UI 設計原則ドキュメント（ui-principles.md）の作成
  - [ ] S3・S4・S8 のシナリオウォークスルー
  - [ ] 技術スタック選定
- 次にやること（優先順）:
  1. `docs/ui-principles.md` を作成する。最初にある程度の全体指針を定めておく（重要な概念を網羅的に。詳細ではなく判断の指針レベル）。S1 で発見済みの原則: 確認ダイアログで危険操作と安全操作を並べない（「保存 or キャンセル」と「破棄 or キャンセル」を分離）
  2. S3（編集・削除）→ S4（一覧）→ S8（リンクから再生）のシナリオウォークスルーを再開。検討方法はシナリオウォークスルー + 構造の要約（`process.md` 4b 参照）
  3. 全シナリオ完了後、画面と遷移の構造を整理して `design.md` に反映
  4. 技術スタック選定（4c）
