# プロジェクト概要

音楽発見体験を記録・検索するための個人用ツール。
詳細は `docs/` 配下のドキュメントを参照。

- ユーザーニーズ: `docs/user-needs.md`
- ユーザーストーリー: `docs/user-stories.md`
- 開発プロセス: `docs/process.md`
- 意思決定ログ: `docs/decisions.md`

## 現在のステータス

- 企画・設計フェーズ: 設計（Step 4）の第1段階が完了、第2段階に着手予定
- MVP は S1（記録）、S3（編集・削除）、S4（一覧）、S8（リンクから再生）の4ストーリー
- S1 は URL のみで保存可能（即時キャプチャ）。曲名・アーティスト名は任意で後から補完
- S2（URL 自動取得）は次期の最優先。未補完レコードの一括取得モードを含む
- 設計文書: `docs/design.md`
- 設計の進め方: 2段階に分けて進行中
  - [x] 第1段階: データモデル + デプロイ形態 — 完了
  - [ ] 第2段階: 画面構成・操作フロー → 技術スタック詳細
- 第1段階で決まったこと:
  - デプロイ形態: ローカル実行・バックグラウンド常駐（軽量サーバー + SQLite）
  - データモデル: 単一 records テーブル + tags テーブル（中間テーブルなし）
  - URL: 一意制約なし（再遭遇記録のため）、可変（リンク切れ対応）
  - タグ: 保存時に lowercase + trim で正規化。候補表示は次期
  - 検索: 完全一致 + FTS5。ベクトル検索は不採用
  - テーブル分割（Song + Encounter）は不採用（同定困難・CRUD複雑化）
  - artist は単一テキストフィールド（LIKE/FTS5 で部分一致検索可能）
- 次にやること:
  - [ ] 第2段階: 画面構成・操作フロー → 技術スタック選定
